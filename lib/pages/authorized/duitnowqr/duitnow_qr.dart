import 'package:flutter/material.dart';
import 'package:mobile/widgets/Background.dart';

class DuitNowQR extends StatelessWidget {
  const DuitNowQR({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [
        Text('duitnow qr page'),
      ],
    );
  }
}